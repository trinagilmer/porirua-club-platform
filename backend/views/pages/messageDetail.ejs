<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head', { title: message.subject || 'Message Detail' }) %>
  <link rel="stylesheet" href="/css/main.css">
  <link rel="stylesheet" href="/css/inbox.css">

  <style>
    /* --- Message Detail Layout --- */
    .message-detail-container {
      max-width: 900px;
      margin: 40px auto;
      background: #fff;
      border-radius: 12px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
      padding: 30px 40px;
    }

    .message-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 20px;
      border-bottom: 1px solid #e5e7eb;
      padding-bottom: 12px;
    }

    .message-header h1 {
      font-size: 1.5rem;
      color: #0f766e;
      margin: 0;
      flex: 1;
    }

    .btn {
      padding: 8px 14px;
      border-radius: 6px;
      font-size: 0.95rem;
      cursor: pointer;
      transition: all 0.2s ease;
      text-decoration: none;
    }

    .btn-primary {
      background: #0f766e;
      color: #fff;
      border: none;
    }
    .btn-primary:hover { background: #115e59; }

    .btn-outline {
      border: 1px solid #0f766e;
      color: #0f766e;
      background: #fff;
    }
    .btn-outline:hover { background: #e6fffa; }

    .message-meta {
      font-size: 0.9rem;
      color: #555;
      margin-bottom: 16px;
      line-height: 1.6;
    }

    .message-body {
      white-space: pre-wrap;
      line-height: 1.6;
      color: #333;
      margin-bottom: 30px;
      font-size: 1rem;
    }

    .linked-banner {
      border-radius: 8px;
      padding: 12px 16px;
      margin: 10px 0 20px;
      font-size: 0.95rem;
    }

    .linked-banner strong {
      color: #0f766e;
    }

    .main-header .logo-img {
      height: 48px !important;
      width: auto !important;
    }
  </style>
</head>

<body>
  <!-- ✅ Header -->
  <%- include('../partials/header', { active: 'inbox', user: user }) %>

  <main class="main-content">
    <div class="message-detail-container">

      <!-- Header -->
      <div class="message-header">
        <h1><%= message.subject || '(No Subject)' %></h1>
        <div class="d-flex gap-2">
          <a href="/inbox" class="btn btn-outline">← Back</a>
          <button class="btn btn-outline" data-bs-toggle="modal" data-bs-target="#linkContactModal">📇 Link Contact</button>
          <button class="btn btn-outline-success" data-bs-toggle="modal" data-bs-target="#linkFunctionModal">🧩 Link Function</button>
          <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#replyMessageModal">💬 Reply</button>
        </div>
      </div>

      <!-- Linked Information Banner -->
      <% if (message.contact_name || message.function_name) { %>
        <div class="alert alert-info linked-banner">
          <% if (message.contact_name) { %>
            📇 Linked to: <strong><%= message.contact_name %></strong>
          <% } %>
          <% if (message.function_name) { %>
            <br>🧩 Function: <strong><%= message.function_name %></strong>
          <% } %>
        </div>
      <% } else { %>
        <div class="alert alert-warning linked-banner">
          ⚠️ This message is not yet linked to any contact or function.
        </div>
      <% } %>

      <!-- Metadata -->
      <div class="message-meta">
        <p><strong>From:</strong> <%= message.from_email || 'Unknown sender' %></p>
        <p><strong>To:</strong> <%= message.to_email || 'Unknown recipient' %></p>
        <p><strong>Date:</strong> <%= message.created_at ? new Date(message.created_at).toLocaleString() : '' %></p>
      </div>

      <!-- Message Body -->
      <div class="message-body">
        <% if (message.body_html) { %>
          <div><%- message.body_html %></div>
        <% } else { %>
          <pre><%= message.body || '(No message body)' %></pre>
        <% } %>
      </div>

      <!-- Attachments -->
      <% if (message.attachments && message.attachments.length > 0) { %>
        <div class="attachments mt-4">
          <h5 class="fw-bold mb-3">📎 Attachments</h5>
          <ul class="list-group list-group-flush">
            <% message.attachments.forEach(a => { %>
              <li class="list-group-item">
                <a href="<%= a.file_url || a.filePath || '#' %>" target="_blank">
                  <%= a.file_name || a.name || "Unnamed attachment" %>
                </a>
              </li>
            <% }) %>
          </ul>
        </div>
      <% } %>

    </div>
  </main>

  <!-- 💬 Reply Message Modal -->
  <div class="modal fade" id="replyMessageModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-lg">
      <div class="modal-content">
        <div class="modal-header" style="background:#0f766e; color:#fff;">
          <h5 class="modal-title">Reply to Message</h5>
          <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>

        <form action="/inbox/reply/<%= message.id %>" method="POST" enctype="multipart/form-data">
          <div class="modal-body">
            <div class="mb-3">
              <label class="form-label">Subject</label>
              <input type="text" name="subject" class="form-control" value="Re: <%= message.subject || '' %>" required>
            </div>

            <div class="mb-3">
              <label class="form-label">Message</label>
              <textarea name="body" rows="6" class="form-control" placeholder="Type your reply..." required></textarea>
            </div>

            <div class="mb-3">
              <label class="form-label">Attachment (optional)</label>
              <input type="file" name="attachment" class="form-control">
            </div>
          </div>

          <div class="modal-footer" style="background:#f8f9fa;">
            <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancel</button>
            <button type="submit" class="btn btn-success">Send Reply</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <!-- ✅ New Modals -->
  <%- include('../partials/linkContactModal', { message, contacts, user }) %>
  <%- include('../partials/linkFunctionModal', { message, functions, user }) %>
<!-- ✅ Bootstrap Toast Container -->
<div class="position-fixed top-0 end-0 p-3" style="z-index: 1080">
  <div id="linkSuccessToast" class="toast align-items-center text-bg-success border-0" role="alert" aria-live="assertive" aria-atomic="true">
    <div class="d-flex">
      <div class="toast-body" id="linkToastBody">
        ✅ Linked successfully!
      </div>
      <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
    </div>
  </div>
</div>

<script>
document.addEventListener("DOMContentLoaded", () => {
  // If a success message is passed via query param, show toast
  const urlParams = new URLSearchParams(window.location.search);
  const successType = urlParams.get("linked");

  if (successType) {
    const toastEl = document.getElementById("linkSuccessToast");
    const toastBody = document.getElementById("linkToastBody");
    const toast = new bootstrap.Toast(toastEl);

    // Customise message depending on what was linked
    if (successType === "contact") toastBody.textContent = "✅ Contact linked successfully!";
    if (successType === "function") toastBody.textContent = "✅ Function linked successfully!";
    if (successType === "both") toastBody.textContent = "✅ Contact & Function linked successfully!";

    toast.show();

    // Clean up URL so toast only shows once
    const cleanUrl = window.location.pathname;
    window.history.replaceState({}, document.title, cleanUrl);
  }
});
</script>


  <%- include('../partials/footer') %>
</body>
</html>
