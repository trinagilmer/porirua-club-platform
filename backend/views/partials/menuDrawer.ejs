
<!-- 🧩 Menu Drawer (Offcanvas Right) -->
<div
  class="offcanvas offcanvas-end"
  tabindex="-1"
  id="menuDrawer"
  aria-labelledby="menuDrawerLabel"
>
  <div class="offcanvas-header border-bottom">
    <h5 id="menuDrawerLabel" class="offcanvas-title">Add / Edit Menu</h5>
    <button
      type="button"
      class="btn-close"
      data-bs-dismiss="offcanvas"
      aria-label="Close"
    ></button>
  </div>

  <div class="offcanvas-body">
    <form id="menuForm">
      <!-- 🔹 Menu Name -->
      <div class="mb-3">
        <label class="form-label fw-semibold">Menu Name</label>
        <input
          type="text"
          name="name"
          class="form-control"
          placeholder="e.g. Buffet Dinner"
          required
        />
      </div>

      <!-- 🔹 Sales Category -->
      <div class="mb-3">
        <label class="form-label fw-semibold">Sales Category</label>
        <select name="category_id" class="form-select" required>
          <% if (categories && categories.length) { %>
            <% categories.forEach(cat => { %>
              <option value="<%= cat.id %>"><%= cat.name %></option>
            <% }) %>
          <% } else { %>
            <option value="">No categories available</option>
          <% } %>
        </select>
      </div>

      <!-- 🔹 Price -->
      <div class="mb-3">
        <label class="form-label fw-semibold">Base Price</label>
        <input
          type="number"
          name="price"
          class="form-control"
          step="0.01"
          min="0"
          placeholder="e.g. 35.00"
        />
      </div>

      <!-- 🔹 Description -->
      <div class="mb-3">
        <label class="form-label fw-semibold">Description</label>
        <textarea
          name="description"
          class="form-control"
          rows="3"
          placeholder="Describe this menu..."
        ></textarea>
      </div>

      <!-- 🔹 Linked Choices -->
      <div class="mb-3">
        <label class="form-label fw-semibold">Linked Choices</label>

        <ul id="choiceList" class="list-group small mb-2">
          <!-- JS will populate linked choices here -->
        </ul>

        <div class="d-flex gap-2">
          <button
            type="button"
            id="addChoiceBtn"
            class="btn btn-sm btn-outline-primary flex-grow-1"
          >
            + Link Existing Choice
          </button>

          <button
            type="button"
            id="createChoiceBtn"
            class="btn btn-sm btn-outline-success flex-grow-1"
          >
            + Create New Choice
          </button>
        </div>
        <div class="mt-3 border rounded-3 p-3 bg-light" id="bulkChoiceCard">
          <div class="d-flex justify-content-between align-items-start mb-2 flex-wrap gap-2">
            <div>
              <strong>Bulk Add Choices</strong>
              <div class="text-muted small">
                Format: <code>Category | Choice | Description | Cost | Price | Unit</code>. Category can be blank to use the menu's category.
              </div>
            </div>
            <button type="button" class="btn btn-sm btn-outline-secondary" id="bulkChoiceClearBtn">Clear</button>
          </div>
          <textarea
            id="bulkChoiceTextarea"
            class="form-control form-control-sm mb-2"
            rows="4"
            placeholder="Catering | Wrap (basic) | | 0.59 | 3.00 | PP&#10;| Cinnamon pinwheel | | 0.15 | 2.50 | PP"
          ></textarea>
          <div class="d-flex justify-content-between align-items-center flex-wrap gap-2">
            <small class="text-muted">Units accept PP, each, etc. Currency symbols are optional.</small>
            <button type="button" class="btn btn-sm btn-primary" id="bulkChoiceAddBtn">Create &amp; Link</button>
          </div>
          <div class="small mt-2 text-muted" id="bulkChoiceStatus"></div>
        </div>
      </div>

      <!-- ?? Add-ons -->
      <div class="mb-3">
        <div class="d-flex justify-content-between align-items-center mb-2">
          <label class="form-label fw-semibold mb-0">Add-ons</label>
          <button
            type="button"
            id="addAddonBtn"
            class="btn btn-sm btn-outline-primary"
          >
            + Add Add-on
          </button>
        </div>
        <ul id="addonList" class="list-group small">
          <!-- JS will populate add-ons here -->
        </ul>
      </div>

      <!-- 🔹 Save -->
      <div class="d-grid mt-4">
        <button type="submit" id="saveMenuBtn" class="btn btn-primary">
          💾 Save Menu
        </button>
      </div>
    </form>
  </div>
</div>
<!-- 🧩 Create Choice Modal -->
<div class="modal fade" id="createChoiceModal" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <form class="modal-content" id="createChoiceForm">
      <div class="modal-header">
        <h5 class="modal-title">Add Choice</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>

      <div class="modal-body">
        <!-- Choice Name -->
        <div class="mb-3">
          <label for="choiceName" class="form-label">Choice name</label>
          <input type="text" class="form-control" id="choiceName" required placeholder="e.g., Beef Main">
        </div>

        <!-- Price + Unit -->
        <div class="row g-3">
          <div class="col-lg-4 col-sm-6">
            <label for="optionPrice" class="form-label">Price</label>
            <input type="number" step="0.01" class="form-control" id="optionPrice" placeholder="e.g., 28.00">
          </div>
          <div class="col-lg-4 col-sm-6">
            <label for="optionCost" class="form-label">Cost</label>
            <input type="number" step="0.01" class="form-control" id="optionCost" placeholder="e.g., 15.00">
          </div>
          <div class="col-lg-4 col-sm-6">
            <label for="optionUnit" class="form-label">Unit</label>
            <select id="optionUnit" class="form-select">
              <option value="">- none -</option>
              <!-- options populated by JS -->
            </select>
          </div>
        </div>
      </div>

      <div class="modal-footer">
        <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancel</button>
        <button type="submit" class="btn btn-primary">Add Choice</button>
      </div>
    </form>
  </div>
</div>
<!-- 🧩 Link Existing Choice Modal -->
<div class="modal fade" id="linkChoiceModal" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered modal-lg">
    <div class="modal-content" id="linkChoiceModalContent">
      <div class="modal-header">
        <h5 class="modal-title">Link Existing Choice</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>

      <div class="modal-body">
        <!-- Search -->
        <div class="row g-2 align-items-center mb-3">
          <div class="col-sm-8">
            <input type="search" id="linkSearch" class="form-control" placeholder="Search choices by name…">
          </div>
          <div class="col-sm-4 text-sm-end">
            <small class="text-muted" id="linkHint">Type to search. Results exclude choices already linked to this menu.</small>
          </div>
        </div>

        <!-- Results -->
        <div id="linkResults" class="list-group border rounded" style="max-height: 340px; overflow: auto;">
          <div class="list-group-item text-muted fst-italic">Start typing to find choices.</div>
        </div>

        <div class="mt-4 pt-3 border-top" id="categoryChoiceSection">
          <div class="d-flex justify-content-between align-items-start mb-2">
            <div>
              <strong>Browse <span id="categoryChoiceLabel">this category</span></strong>
              <div class="text-muted small">Select multiple choices to link at once.</div>
            </div>
            <button type="button" class="btn btn-sm btn-outline-secondary" id="categoryChoiceRefresh">
              Refresh
            </button>
          </div>
          <div class="form-check form-check-sm mb-2">
            <input class="form-check-input" type="checkbox" id="categoryChoiceSelectAll" disabled>
            <label class="form-check-label" for="categoryChoiceSelectAll">
              Select all
            </label>
          </div>
          <div id="categoryChoiceList" class="list-group border rounded small" style="max-height: 260px; overflow: auto;">
            <div class="list-group-item text-muted fst-italic">Load category choices to start.</div>
          </div>
          <div class="d-flex justify-content-between align-items-center gap-2 mt-2">
            <button type="button" class="btn btn-sm btn-outline-secondary" id="categoryChoiceClear">Clear Selected</button>
            <button type="button" class="btn btn-sm btn-primary" id="categoryBulkLinkBtn" disabled>Link Selected</button>
          </div>
          <div id="categoryChoiceStatus" class="small mt-2 text-muted"></div>
        </div>
      </div>

      <div class="modal-footer">
        <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Close</button>
      </div>
    </div>
  </div>
</div>

<!-- ?? Add-on Modal -->
<div class="modal fade" id="addonModal" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <form class="modal-content" id="addonForm">
      <div class="modal-header">
        <h5 class="modal-title" id="addonModalTitle">Add Add-on</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>

      <div class="modal-body">
        <input type="hidden" id="addonId">
        <div class="mb-3">
          <label for="addonName" class="form-label">Add-on name</label>
          <input type="text" class="form-control" id="addonName" placeholder="e.g., Beverage Package" required>
        </div>

        <div class="row g-3">
          <div class="col-md-6">
            <label for="addonPrice" class="form-label">Price</label>
            <input type="number" step="0.01" class="form-control" id="addonPrice" placeholder="e.g., 12.50">
          </div>
          <div class="col-md-6">
            <label for="addonUnit" class="form-label">Qty type</label>
            <select id="addonUnit" class="form-select">
              <option value="">No unit</option>
            </select>
          </div>
        </div>

        <div class="form-check form-switch my-3">
          <input class="form-check-input" type="checkbox" role="switch" id="addonEnableQty">
          <label class="form-check-label" for="addonEnableQty">Allow manual quantity</label>
        </div>

        <div class="mb-0">
          <label for="addonDefaultQty" class="form-label">Default quantity</label>
          <input type="number" step="1" min="1" class="form-control" id="addonDefaultQty" placeholder="Defaults to 1" disabled>
        </div>
      </div>

      <div class="modal-footer">
        <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancel</button>
        <button type="submit" class="btn btn-primary">Save Add-on</button>
      </div>
    </form>
  </div>
</div>


