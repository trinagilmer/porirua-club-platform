===============
// 🧭 FUNCTION PAGE LAYOUT — Sidebar + Content Alignment
// Matches Communications page spacing and proportions
// ===========================================================

.function-layout-two-col {
  display: flex;
  align-items: flex-start;
  justify-content: flex-start;
  gap: 1.25rem; /* consistent spacing between sidebar and workspace */
  margin: 0;
  padding: 0 1rem;
}

/* 🧩 Sidebar */
.function-sidebar {
  flex: 0 0 280px;
  background: $color-bg-panel;
  border: 1px solid $color-border;
  border-radius: $radius;
  padding: 1.5rem 1.25rem 2rem;
  box-shadow: $shadow-sm;
  margin-top: 0;
  position: relative;
  top: 0;

  h2 {
    font-size: 1.35rem;
    font-weight: 600;
    color: #333;
    margin-bottom: 1rem;
  }

  label {
    font-weight: 500;
    color: #555;
    font-size: 0.9rem;
    margin-bottom: 0.25rem;
  }

  input,
  select,
  button {
    font-size: 0.9rem;
    padding: 0.45rem 0.6rem;
    border-radius: 6px;
  }

  .btn-outline-primary {
    border-color: $primary;
    color: $primary;

    &:hover {
      background-color: $primary;
      color: #fff;
    }
  }
}

/* 🧩 Workspace Area */
.function-workspace {
  flex: 1;
  background: $color-bg-panel;
  border-radius: $radius;
  padding: 1.75rem 2rem;
  box-shadow: $shadow-sm;
  margin-top: 0;

  h1 {
    font-size: 1.75rem;
    font-weight: 600;
    color: #333;
    margin-bottom: 1.25rem;
  }

  .btn-primary,
  .btn-success {
    background-color: $primary;
    border-color: $primary;

    &:hover {
      background-colour: color.adjust($primary, $lightness: -10%);
      border-colour: color.adjust($primary, $lightness: -10%);
    }
  }
}

.function-tabs {
  margin-top: 0 !important;
  padding-bottom: 1rem;
}
/* =========================================================
   🧭 HEADER / NAVBAR — Porirua Club Brand Styling
   ========================================================= */

.main-header.navbar {
  background-color: #000; /* dark base */
  border-bottom: 3px solid #6BB4DE; /* brand accent border */
  padding: 0.75rem 1.25rem;
  box-shadow: 0 2px 6px rgba(0,0,0,0.25);
  z-index: 1030;

  .navbar-brand {
    display: flex;
    align-items: center;
    img.logo-img {
      height: 42px;
      transition: transform 0.3s ease;
      &:hover {
        transform: scale(1.05);
      }
    }
  }

  /* 🍔 Burger icon */
  .navbar-toggler {
    border: none;
    background: none;
    &:focus {
      outline: none;
      box-shadow: none;
    }
    .navbar-toggler-icon {
      filter: invert(1); /* white burger */
    }
  }

  /* 🔹 Nav links */
  .nav-link {
    color: #f8f9fa;
    font-weight: 500;
    margin-right: 0.75rem;
    transition: color 0.2s ease, border-color 0.2s ease;

    &:hover,
    &.active {
      color: #6BB4DE !important;
      border-bottom: 2px solid #6BB4DE;
    }
  }

  /* 👤 User circle */
  .user-circle {
    background: #6BB4DE;
    color: #fff;
    font-weight: 600;
    border-radius: 50%;
    width: 36px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1rem;
    cursor: default;
  }

  .btn-outline-light.btn-sm {
    border-color: #6BB4DE;
    color: #6BB4DE;
    &:hover {
      background: #6BB4DE;
      color: #fff;
    }
  }
}

/* Ensure sticky behavior if needed */
.main-header.navbar {
  position: sticky;
  top: 0;
}

.main-header {
  background-color: $deep;
  color: $white;
  padding: 0.75rem 1rem;

  .logo-img {
    height: 36px;
  }

  .nav-link {
    color: $white;
    font-weight: 500;
    margin-right: 1rem;

    &.active,
    &:hover {
      color: $primary;
    }
  }

  .user-circle {
    background-color: $primary;
    color: $white;
    border-radius: 50%;
    width: 36px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 600;
  }
}

.main-content {
  padding: 2rem;
  background: $gray-100;
}